{
  "schemaVersion": "1.1",
  "description": "Quality gates for PRIME DIRECTIVE Phase 2 spec validation. Every spec must pass all gates to be considered reconstruction-complete. The self_improvement_rules section defines the MANDATORY autonomous loop that any AI must execute until thresholds are met.",
  "gates": {
    "L1_structural": {
      "description": "Every IR field has a spec counterpart",
      "checks": [
        "Every detailedVariable (except UberGraphFrame) appears in spec Variables section",
        "Every detailedFunction (except ExecuteUbergraph_* and trivial UserConstructionScript) appears in spec Functions section",
        "Every detailedComponent appears in spec Components section",
        "Every implementedInterface is mentioned in spec",
        "Every classDefaultValueDelta property appears in CDO Overrides section",
        "Every dependencyClosure.moduleNames appears in Dependencies section",
        "compilerIRFallback.hasUnsupportedNodes status noted in IR Notes",
        "Identity section exists with Path, Parent, Generated Class (## Identity heading OR **Path:**/**Parent:** bold fields)",
        "Purpose section exists"
      ],
      "required": true,
      "error_threshold": 0,
      "notes": "validate_specs.py implements these checks. UserConstructionScript is excluded from mandatory L1 check — include in spec only when it has authored logic. Identity accepted as ## Identity heading OR **Path:** + **Parent:** bold fields."
    },
    "L2_accuracy": {
      "description": "Types, defaults, flags match the IR exactly",
      "checks": [
        "Variable types match IR typeCategory + typeObjectPath",
        "Replicated variables mention replication in spec",
        "Network functions (Server/Client/Multicast) mention RPC type in spec",
        "Override functions mention override status",
        "Default values for key properties match IR"
      ],
      "required": true,
      "error_threshold": 0
    },
    "L3_logic_coverage": {
      "description": "Every graph entry point and branch is represented in pseudo-code",
      "checks": [
        "Every K2Node_Event in EventGraph has a corresponding section in spec Logic",
        "Every K2Node_FunctionEntry for user functions has logic description",
        "Branch conditions (K2Node_IfThenElse) are described with their conditions",
        "Spec line count is proportional to graph complexity (not a 2-line summary of 50 exec edges)",
        "Key function calls (meta.functionName) appear in pseudo-code"
      ],
      "required": true,
      "warning_threshold": 3,
      "error_threshold": 0
    },
    "L4_reconstruction": {
      "description": "An AI reading ONLY the spec can reconstruct a functionally equivalent Blueprint",
      "checks": [
        "AI reconstruction produces matching variable declarations (count, names, types)",
        "AI reconstruction produces matching function signatures",
        "AI reconstruction produces equivalent logic flow",
        "AI reconstruction produces matching component hierarchy",
        "AI reconstruction identifies same C++ dependencies"
      ],
      "required": false,
      "notes": "L4 is AI-driven and expensive. Run selectively on high-priority specs or after L1-L3 pass."
    },
    "L5_convergence": {
      "description": "C++ from spec matches C++ from direct IR conversion",
      "checks": [
        "Path A (IR → C++ converter) and Path B (IR → spec → AI C++) produce equivalent headers",
        "Path A and Path B produce equivalent function implementations",
        "Both paths reference the same #includes and module dependencies"
      ],
      "required": false,
      "notes": "L5 requires the C++ codegen tool (not yet built). This is the ultimate quality bar."
    }
  },
  "corpus_thresholds": {
    "L1_pass_rate_minimum": 95.0,
    "L2_pass_rate_minimum": 90.0,
    "L3_pass_rate_minimum": 85.0,
    "total_error_budget": 50,
    "notes": "These thresholds gate whether a corpus-wide spec generation run is acceptable. Individual failures are logged to _VALIDATION_FAILURES.md for re-generation. The loop runs until ALL thresholds are met."
  },
  "self_improvement_rules": {
    "failure_pattern_threshold": 5,
    "description": "MANDATORY AUTONOMOUS LOOP: Any AI receiving this context MUST run this loop until corpus_thresholds are met. The loop is not optional — a spec corpus that has not passed validation has not fulfilled the PRIME DIRECTIVE.",
    "mandatory_loop": {
      "trigger": "After any spec generation run, OR at the start of any session where pass rates are below thresholds",
      "stopping_condition": "L1 pass rate >= 95% AND L2 pass rate >= 90% AND L3 pass rate >= 85%",
      "steps": [
        "STEP 1 — VALIDATE: Run validate_specs.py to produce _VALIDATION_FAILURES.md. Read the Summary section. If thresholds met, STOP.",
        "STEP 2 — ANALYZE: Read _VALIDATION_FAILURES.md. Count unique error patterns. Note any pattern appearing 5+ times.",
        "STEP 3 — UPDATE PLAYBOOK: For each pattern appearing 5+ times that lacks a CRITICAL RULE in ANALYSIS_PLAYBOOK.md, add one. Commit: 'Playbook: add rule for [pattern]'",
        "STEP 4 — REGENERATE: Fix failing specs ordered by error count (highest first). Fully regenerate from JSON IR — do NOT spot-patch. Batch 10-20 specs per commit.",
        "STEP 5 — COMMIT: git commit with pass rate delta in message: 'Spec: iteration N — M specs fixed (X% → Y%)'",
        "STEP 6 — LOOP: Go to STEP 1."
      ]
    },
    "playbook_update_protocol": {
      "when": "Same failure type appears in 5+ specs",
      "what": "Add a new CRITICAL RULE to ANALYSIS_PLAYBOOK.md describing: what triggers the error, how to avoid it, what correct spec content looks like",
      "commit_message_format": "Playbook: add CRITICAL RULE for [error pattern]",
      "goal": "By iteration 3-5, ANALYSIS_PLAYBOOK.md should be comprehensive enough that a fresh AI generates near-100% passing specs WITHOUT needing the loop"
    },
    "known_resolved_patterns": [
      {
        "pattern": "UserConstructionScript missing from spec",
        "resolution": "Validator updated (v1.1) to exclude UserConstructionScript from L1 mandatory checks. ANALYSIS_PLAYBOOK.md RULE 4 documents the include-only-if-authored-logic rule.",
        "resolved_in": "validate_specs.py 2026-03-01"
      },
      {
        "pattern": "Spec has no Identity section",
        "resolution": "Validator updated (v1.1) to accept **Path:** + **Parent:** bold-field format as equivalent to ## Identity heading. ANALYSIS_PLAYBOOK.md RULE 1 documents preferred ## Identity format.",
        "resolved_in": "validate_specs.py 2026-03-01"
      }
    ]
  },
  "current_status": {
    "as_of": "2026-03-01",
    "pass_rate_L1": 80.1,
    "passing": 386,
    "total": 482,
    "failing": 96,
    "top_remaining_issues": [
      "Missing variables (bulk of failures — incomplete variable tables)",
      "Missing CDO sections (BTS_CheckAmmo, BTS_SetFocus, BTS_Shoot, etc.)",
      "Missing CDO properties (individual properties absent from CDO tables)",
      "Missing user functions (non-trivial functions not listed)",
      "Missing Logic sections (BPs with nodes but no ## Logic section)",
      "Missing module dependencies"
    ],
    "next_action": "See ITERATION_LOOP.md — start at STEP 1 (run validator) and follow the loop"
  }
}
